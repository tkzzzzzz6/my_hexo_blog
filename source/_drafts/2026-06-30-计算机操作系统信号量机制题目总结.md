1. ![1782814814574.png](https://tk-pichost-1325224430.cos.ap-chengdu.myqcloud.com/blog/1782814814574.png)
```cpp

semaphore a,b,c,d = 0,0,0,0;
A(){
    执行A;
    signal(a);
}

B(){
    执行B;
    signal(b);
}

C(){
    wait(a);
    wait(b);
    执行C;
    signal(c);
}

D(){
    执行D;
    signal(d);
}

E(){
    wait(c);
    wait(d);
    执行E;
}

```

2. ![1782814978202.png](https://tk-pichost-1325224430.cos.ap-chengdu.myqcloud.com/blog/1782814978202.png)

```cpp
semaphore plate = 1,orange = 0,apple = 0;

father(){
    while(1){
        p(plate);
        放苹果;
        v(apple);
    }
}

mother(){
    while(1){
        p(plate);
        放橘子;
        v(orange);
    }
}

son(){
    while(1){
        p(orange);
        拿橘子;
        v(plate);
        吃橘子;
    }
}

daughter(){
    while(1){
        p(apple);
        拿苹果;
        v(plate);
        吃苹果;
    }
}

```

3. ![1782815531383.png](https://tk-pichost-1325224430.cos.ap-chengdu.myqcloud.com/blog/1782815531383.png)

```cpp
semaphore empty = 10,orange = 0,apple = 0,mutex = 1;

father(){
    while(1){
        p(empty);
        p(mutex);
        放水果;
        v(mutex);
        
        if(水果是苹果){
            v(apple);
        }else{
            v(orange);
        }
    }
}

son(){
    while(1){
        p(orange);
        p(mutex);
        拿橘子;
        v(mutex);
        v(empty);
        
        吃橘子;
    }
}

daughter(){
    while(1){
        p(orange);
        p(mutex);
        拿苹果;
        v(mutex);
        v(empty);

        吃苹果;
    }
}
```

4. ![1782816605246.png](https://tk-pichost-1325224430.cos.ap-chengdu.myqcloud.com/blog/1782816605246.png)
![1782816687390.png](https://tk-pichost-1325224430.cos.ap-chengdu.myqcloud.com/blog/1782816687390.png)
```cpp
semaphore close_door = 0,stop_bus = 0;
driver(){

    p(close_door);
    启动车辆
    正常行车
    到站停车
    v(stop_bus);

}

conductor(){

    关车门
    v(close_door);
    售票
    p(stop_bus);
    开车门

}
```

5. ![1782817357958.png](https://tk-pichost-1325224430.cos.ap-chengdu.myqcloud.com/blog/1782817357958.png)
![1782817400470.png](https://tk-pichost-1325224430.cos.ap-chengdu.myqcloud.com/blog/1782817400470.png)
```cpp
semaphore empty = n,full = 0,mutex = 1;

producer(){
    while(1){
        生产产品
        wait(empty);
        wait(mutex);
        放产品
        signal(mutex);
        signal(full);
    }
}

consumer(){
    while(1){
        wait(full);
        wait(mutex);
        拿产品
        signal(mutex);
        signal(empty);

        消费产品
    }
}
```
- 书上代码
```cpp
int in=0,out=0;
item buffer[n];
semaphore mutex =1,empty = n,full = 0;

//生产者
void producer(){
    do{
        producer an item nextp;
        ···
        wait(empty);  //询问是否有空缓存区
        wait(mutex); //互斥访问
        buffer[in] = nextp;
        in = (in+1)%n;
        signal(mutex);//解除互斥访问
        signal(full); //释放一个满缓存区，满缓存+1
    }while(TRUE)
}

//消费者
void consumer(){
    do{
        wait(full);
        wait(mutex);
        nextc = buffer[out];
        out = (out+1) %n;
        signal(mutex);
        signal(empty);
    }while(TRUE)
}

```

6. ![1782817956600.png](https://tk-pichost-1325224430.cos.ap-chengdu.myqcloud.com/blog/1782817956600.png)
![1782817999976.png](https://tk-pichost-1325224430.cos.ap-chengdu.myqcloud.com/blog/1782817999976.png)

- 原版会产生死锁代码
```cpp
semaphore chopstick[5] = {1,1,1,1,1};

people(i){
    while(1){
        思考

        wait(chopstick[i]);
        wait(chopstick[(i+1)%5]);
        就餐
        signal(chopstick[i]);
        signal(chopstick[(i+1)%5]);

        思考
    }
}
```

- 改进1:偶数号先拿左后拿右,奇数号先拿右后拿左
```cpp
semaphore chopstick[5] = {1,1,1,1,1};

people(i){
    while(1){
        思考

        if(i % 2 == 0){
            wait(chopstick[i]);
            wait(chopstick[(i+1)%5]);
        }else{
            wait(chopstick[(i+1)%5]);
            wait(chopstick[i]);
        }

        就餐
        signal(chopstick[i]);
        signal(chopstick[(i+1)%5]);

        思考
    }
}
```

- 改进2:最多只能4个哲学家同时就餐
```cpp
semaphore chopstick[5] = {1,1,1,1,1};
semaphore room = 4;

people(i){
    while(1){
        思考

        wait(room);
        wait(chopstick[i]);
        wait(chopstick[(i+1)%5]);
        就餐
        signal(chopstick[i]);
        signal(chopstick[(i+1)%5]);
        signal(room);

        思考
    }
}
```

7. ![1782818552346.png](https://tk-pichost-1325224430.cos.ap-chengdu.myqcloud.com/blog/1782818552346.png)
```cpp
semaphore w  = 1;      // 排队信号量：读者和写者都先来后到
semaphore wr = 1;      // 读写互斥：保证写者独占或读者群体独占
semaphore mutex = 1;   // 保护 count
int count = 0;

// 写者
writer() {
    while (1) {
        wait(w);       // 排队
        wait(wr);      // 申请独占
        写文件;
        signal(wr);    // 释放独占
        signal(w);     // 出队（原代码这里错写成 wait）
    }
}

// 读者
reader() {
    while (1) {
        wait(w);               // 排队
        wait(mutex);           // 保护计数器
        count++;
        if (count == 1)        // 第一个读者负责封锁写者
            wait(wr);
        signal(mutex);
        signal(w);             // 出队，让后面排队的人竞争

        读文件;

        wait(mutex);
        count--;
        if (count == 0)         // 最后一个读者负责释放写者
            signal(wr);
        signal(mutex);
    }
}

```

8. ![1782819450027.png](https://tk-pichost-1325224430.cos.ap-chengdu.myqcloud.com/blog/1782819450027.png)
```cpp
semaphore empty = 5,orange = 0,apple = 0,mutex = 1;

father(){
    while(1){
        wait(empty);
        wait(mutex);
        放水果
        signal(mutex);

        if(放的是苹果){
            signal(apple);
        }else{
            signal(orange);
        }
    }
}

son(){
    while(1){
        wait(orange);
        wait(mutex);
        拿橘子
        signal(mutex);
        signal(empty);

        吃橘子
    }
}

daughter(){
    while(1){
        wait(apple);
        wait(mutex);
        拿苹果
        signal(mutex);
        signal(empty);

        吃苹果
    }
}
```

9. ![1782819777301.png](https://tk-pichost-1325224430.cos.ap-chengdu.myqcloud.com/blog/1782819777301.png)
```cpp
semaphore customers = 0;
semaphore barbers = 0;
semaphore mutex = 1;

int waiting = 0;
const int N = 等候椅数;

barber(){

    while(1){

        wait(customers);

        wait(mutex);

        waiting--;

        signal(barbers);

        signal(mutex);

        cut_hair();

    }
}

customer(){

    wait(mutex);

    if(waiting < N){

        waiting++;

        signal(customers);

        signal(mutex);

        wait(barbers);

        get_haircut();

        pay();

        leave();

    }
    else{

        signal(mutex);

        leave();

    }

}
```

10. ![1782819880543.png](https://tk-pichost-1325224430.cos.ap-chengdu.myqcloud.com/blog/1782819880543.png)
```cpp
semaphore rope = 1;
semaphore Aturn = 1;
semaphore Bturn = 0;

A(){

    while(1){

        wait(Aturn);

        wait(rope);

        从A端走向B端();

        signal(rope);

        signal(Bturn);

    }

}

B(){

    while(1){

        wait(Bturn);

        wait(rope);

        从B端走向A端();

        signal(rope);

        signal(Aturn);

    }

}
```

> 总结:同步的信号量一开始是0,互斥的信号量是定值